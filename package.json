{
  "name": "socket-rpc",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "generate": "bun run generator/index.ts",
    "generate:watch": "bun run generator/index.ts --watch",
    "server": "cd pkg/server && bun run index.ts",
    "plugin": "cd pkg/plugin && bun run dev"
  },
  "workspaces": [
    "pkg/*",
    "generator"
  ],
  "peerDependencies": {
    "typescript": "^5"
  }
}